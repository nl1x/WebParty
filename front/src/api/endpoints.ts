const Endpoints = {
    auth: {
        login: '/auth/login',
    },
    users: {
        me: '/users/me',
    },
    actions: {
        validate: '/actions/validate-current',
    }
}

export default Endpoints;
